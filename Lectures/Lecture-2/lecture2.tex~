\lecture{2}{15 August 2018}

From last time, we recall a few facts about Schonflies theorems.

\begin{theorem}[Schonflies theorems]
  A loally flat $S^{n-1} \subset S^{n}$ always bounds a ball.
  \begin{itemize}
  \item Schonflies, in 1906 for $n = 2$.
  \item Alexander, in 1924 for $n = 3$.
  \item Mazur, in 1959 for $n \geq 4$.
  \end{itemize}
\end{theorem}

A \textsc{smooth} $S^{n-1} \subset S^{n}$ bounds a ball if $n \neq 4$.

\section{Examples of knots and links}
\label{sec:examples-knots-links}

\subsection{Torus links $T_{p,q}$}
\label{sec:torus-links-t_p}

Consider $U \subset S^{3}$.  Let $V = \nu(U)$, and let $T = \partial V \cong T^{2}$.

\begin{exercise}
  How many components does $T_{p,q}$ have?  Use Seifert-van Kampen to give a presentation of $\pi_{1}(S^{3} \ T_{p,q})$.
\end{exercise}

\subsection{Satellite links}
\label{sec:satellite-links}

Fix $K \subset S^{3}$.  Let $V = S^{1} \times D^{2}$, let $P \subset V$ be an embedded, closed $1$-manifold.

\subsection{Rational links}
\label{sec:rational-links-kcd}

Let $c/d \in \Q \cup \{\infty\}$.

\begin{theorem}[Thurston, 1982]
  Every prime know is either a torus knot, a proper satellite knot, or a hyperbolic knot.
\end{theorem}


%%% Local Variables:
%%% mode: latex
%%% TeX-master: "../../trisection-notes"
%%% End:
